# Second brain

Private scan notes belong in `private/`, which is gitignored.

This public repository must not contain HubSpot contacts, inbox threads, investor email addresses, cap-table figures, or the execution ledger.

The tracked funding research lives in `../research/`.
